{
  "name": "nortabs-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Thin LLM passthrough + metadata cache for NorTabs UG-import enrichment. See PLAN.md 'Phase 2.5'.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  }
}
